"""Read-only IO adapters — empirical-priors addendum §7.

  cli_bridge.py — sanctioned polymarket-cli wrapper for ad-hoc agent
                  use during development. ALLOW-LISTED commands only;
                  no order placement.
"""

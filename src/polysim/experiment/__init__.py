"""Pre-registered experiment harness — empirical-priors addendum §8.

  hypotheses.py — H1..H6 statistical tests (scipy / statsmodels, no hand rolls)
  baselines.py  — four baselines (random cohort, top-historical-P&L,
                  buy-favorite, inverse classifier)
  reporting.py  — end-of-experiment Markdown rendering
"""
